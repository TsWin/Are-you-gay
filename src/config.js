const config = {
    defaultPath: '/',
    defaultLanguage: 'fr',
    languages: ['en', 'fr'],
    languagesNames: {
        'en': 'English',
        'fr': 'Français'
    },
};

export default config;
